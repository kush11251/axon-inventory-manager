# Axon Inventory Manager
A scalable, real-time inventory management system with data analytics capabilities

## Getting Started
1. Run `go mod init axon-inventory-manager` in the terminal to initialize the Go module
2. Run `go mod tidy` to download dependencies
3. Execute the system using `go run main.go`